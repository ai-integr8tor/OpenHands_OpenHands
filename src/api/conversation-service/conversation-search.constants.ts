/** Default number of rows shown before the user types a query. */
export const CONVERSATION_SEARCH_RECENT_LIMIT = 50;

/**
 * Max conversations requested for a title-filtered search.
 * The command menu only surfaces a small slice; keep the request bounded.
 */
export const CONVERSATION_SEARCH_RESULT_LIMIT = 50;
